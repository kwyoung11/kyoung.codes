import React from 'react';
import styled from 'styled-components';

interface MetaPostProps {
  tags: string[];
}
export const MetaPost = (props) => {
  return <StyledMetaPost></StyledMetaPost>;
};

const StyledMetaPost = styled.div``;
